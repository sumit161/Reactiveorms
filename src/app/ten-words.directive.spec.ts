import { TenWordsDirective } from './ten-words.directive';

describe('TenWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new TenWordsDirective();
    expect(directive).toBeTruthy();
  });
});
